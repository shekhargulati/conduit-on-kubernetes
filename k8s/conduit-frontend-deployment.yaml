apiVersion: apps/v1
kind: Deployment
metadata:
  name: conduit-frontend
spec:
  replicas: 2
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: conduit-frontend
    spec:
      containers:
        - image: com.conduitapp/frontend
          imagePullPolicy: Never
          name: conduit-frontend
          ports:
            - containerPort: 80